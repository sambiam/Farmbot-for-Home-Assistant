# Shared test/CI dependency pins, single source of truth for the syntax
# and pytest workflow jobs. Kept aligned with Home Assistant Core 2026.7.x's
# homeassistant/package_constraints.txt (voluptuous, requests, aiohttp,
# Pillow) and custom_components/farmbot/manifest.json (requests, paho-mqtt).
#
# Pillow is NOT a manifest requirement: Home Assistant Core provides and pins
# it (Pillow==12.2.0 in Core's package_constraints.txt for 2026.7.x). We list
# the same version here only so the standalone test environment has the PIL
# module the image_utils tests import -- it mirrors the Core-provided version,
# it does not re-declare a dependency the integration installs.
requests==2.34.2
paho-mqtt==2.1.0
voluptuous==0.15.2
aiohttp==3.14.1
Pillow==12.2.0
